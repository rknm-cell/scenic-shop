import React from "react";
import { NavBar } from "./NavBar";

const Home = ({}) => {
  return (
    <div>
      <NavBar />
      <h1>Fab Shop</h1>
      <p>This is the fab shop page</p>
    </div>
  );
};

export default Home;
